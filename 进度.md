# 多模态课件知识抽取项目进度

更新日期：2026-08-19

## 1. 当前目标与技术路线

本项目当前只负责知识抽取阶段：将用户上传的 PDF、PPTX 等课件转换为可检查的多模态抽取结果，以及可供后续知识图谱构建和检索使用的统一节点与向量。

当前模型配置：

- 图片理解、公式、表格、代码识别：千问 API `qwen3.7-plus`
- 文本向量：千问 API `qwen3.7-text-embedding`
- 向量维度：1024
- 本地模型：当前主流程不使用本地 DeepSeek-VL
- 思考模式：已关闭，适合结构化抽取并减少输出 token

启动方式：

```powershell
cd extraction-main
.\extraction\.venv_api\Scripts\python.exe .\extraction\run_src2.py
```

输入目录为 `extraction/input/`。每次运行会创建新的 `extraction/output/qwen_qwen3.7-plus_N/`，序号取现有最大值加一，不覆盖或混入旧结果。

## 2. 输出结构

每次运行结果分为正式交付数据和人工调试数据：

```text
extraction/output/qwen_qwen3.7-plus_N/
  kg_data/                 # 下游知识图谱和检索正式输入
    course_manifest.json
    documents.json
    pages.json
    content_list.json
    multimodal_nodes.json
    relationships.json
    schema.json
    knowledge_export_summary.json
    vectors/
      vector_index.json
      vector_matrix.npy
  debug/                   # 原始抽取结果和人工检查数据
    text/
    images/
    formulas/
    tables/
    code/
    logs/
    document_metadata/
    professional_terms_library.json
  ex_items/                # 图片理解失败或课程无关图片
  formula_review_items/    # 不确定、残缺或低置信度公式
  table_review_items/      # API 失败或结构异常表格
```

## 3. 最近三轮运行概况

| 版本 | 主要结果 | 暴露的问题 | 处理状态 |
|---|---|---|---|
| `_4` | 3 个文档；实际 43 页；165 个向量 | `pages.json` 少一页；21 个公式反斜杠被破坏；3 个密集公式 JSON 截断；设备表格 API 超时；文本包含邮箱和页脚 | 已修复主要代码问题 |
| `_5` | 43 页；181 个节点和向量；公式不再损坏；图片理解全部成功 | 49 个 PPT 公式中混入 3 个代码表达式和 2 个变量标签；PDF 代码图片未进入 CodeBlock；图片描述偏长；VLM 消耗约 185500 token | 路由、提示词和成本问题已修复 |
| `_6` | 43 页；169 个节点和向量；新增 PDF 代码；设备表格成功；约 6 分 28 秒完成 | PPT 第 14 页公式达到 1600 token 后截断；第 15 页公式续行被拆分；设备表格合并表头列宽不一致 | 代码已修改，等待 `_7` 验证 |

## 4. 已发现问题与修改方式

### 4.1 输出覆盖和目录混用

问题：早期相同模型重复运行时会写入同一目录。同名文件会覆盖，但旧的无效文件可能残留，导致不同运行结果混在一起。

修改：输出目录改为 `模型名称_递增序号`，例如 `qwen_qwen3.7-plus_6`。每次运行创建全新目录，不删除、不覆盖历史结果。同时将正式交付数据放入 `kg_data/`，人工检查内容放入 `debug/`。

### 4.2 图片中黑块、校徽、水印和无意义小图过多

问题：PDF/PPTX 内部包含大量黑色遮罩、小图标、校徽、水印和重复资源，早期会全部进入图片理解。

修改：增加图片面积、颜色方差、黑白比例和边缘信息检测；增加跨页图片签名去重；重复出现的校徽和水印不再进入 API。当前图片结果已经较干净。

### 4.3 图片理解失败的人工处理

问题：API 失败、空描述或与课程无关的图片需要由前端展示并人工修改，不能进入正式图谱数据。

修改：建立 `ex_items/`。待审核图片保存预览图和 metadata，包含失败原因、来源页、模型、状态和原始路径；待审核内容不会进入 `kg_data/`。

### 4.4 LaTeX 被误当作 Windows 路径

问题：保存 JSON 时，通用路径标准化逻辑把任何包含反斜杠的字符串都视为路径，导致 `\frac`、`\theta`、`\omega` 被保存成 `/frac`、`/theta`、`/omega`。`_4` 中 39 个公式有 21 个受到影响。

原因：Windows 下以单个反斜杠开头的字符串也可能被 `os.path.isabs()` 判为绝对路径。

修改：路径标准化只处理明确的路径字段、盘符路径、UNC 路径和已知项目目录前缀，不再根据“包含反斜杠”判断。已通过 LaTeX 和真实路径的针对性测试，`_5`、`_6` 中该问题未再次出现。

### 4.5 密集公式 JSON 截断

问题：PPT 第 13 至 15 页包含较多公式。早期公式接口只允许 800 token，模型响应在 JSON 中间截断，旧解析器会丢弃已经完整返回的公式。

修改过程：

1. 将公式输出上限从 800 提升至 1600 token。
2. 增加部分 JSON 恢复，可从截断响应中恢复已经闭合的公式对象。
3. 恢复对象标记为 `partial_json_response` 并进入 `formula_review_items/`，不会直接进入正式数据。
4. `_6` 第 14 页仍正好达到 1600 token，因此最新代码将上限提高到 2000。
5. 最新公式提示词只要求 `latex`、`uncertain_symbols`、`confidence`，不再让抽取模型生成逐符号解释和条件，减少截断与推测。该修改等待 `_7` 验证。

### 4.6 公式节点混入代码和变量标签

问题：`_5` 的 PPT 第 6 页把 `dt = ...`、`pulse2Send += ...` 等代码表达式重复建成 Formula；`u_s(t)`、`\widetilde U_s(s)` 等单独标签也被当作完整公式。

修改：

- 图片模态路由改为代码优先、表格其次、公式最后。
- 代码图片不再同时进入公式识别。
- 公式校验增加 `not_substantive_formula`，单独变量或函数标签进入审核或被排除。
- `_6` 中代码表达式和变量标签已经不再进入正式公式节点。

### 4.7 同一公式的续行被拆开

问题：`_6` 第 15 页中以 `=` 和 `\Rightarrow` 开头的内容被模型拆成独立公式，造成一条推导被分成三条节点。

修改：公式校验前增加续行合并。同一来源图片中，以 `=`、`\Rightarrow`、`\Leftrightarrow` 等开头的条目自动并入上一条公式，并合并不确定字符和最低置信度。对 `_6` 数据的本地测试可将该页 10 项合理合并为 8 项，等待 `_7` 实际验证。

### 4.8 表格 API 超时

问题：PDF 第 11 页 DM432C 驱动器照片包含两张密集且旋转的参数表。`_4`、`_5` 中表格请求连续超时，一次页面可能等待数分钟。

修改：

- 表格最大输出调整为 1500 token。
- 表格请求单次超时设置为 150 秒，只重试一次。
- 失败结果保存到 `table_review_items/`，不污染正式数据。
- 关闭思考模式后，`_6` 中该表格成功返回，不再超时。

后续可以增加自动旋转、表格区域定位和裁切，并支持只重跑审核项，无需重新处理整个项目。

### 4.9 多级表头结构不一致

问题：`_6` 的 DM432C 表格数据值和 SW1 至 SW8 组合基本准确，但第一层表头返回 8 列，第二层表头及数据是 10 列；旧校验器由于整体一致率较高，仍错误标记为通过。

修改：

- 表格校验增加严格行宽检测，无法解释的非矩形表格进入审核。
- 如果 `merged_cells` 坐标完整，且非空标题数量与合并区域数量一致，则自动重建缺失的空占位列。
- 本轮表格可安全恢复为 10 列：`Current Table` 位于第 1 列，`Pulse/rev Table` 位于第 6 列。
- 自动修复写入 `auto_repairs`，便于人工追踪。该修改等待 `_7` 验证。

### 4.10 PDF 代码图片没有进入 CodeBlock

问题：PPTX 分支支持图片代码专用识别，但 PDF 分支早期只执行图片描述、公式和表格识别。`w08` 第 8 页 Arduino 代码虽然被图片描述识别，却没有正式 CodeBlock。

修改：PDF 和 PPTX 统一使用代码图片路由和 `recognize_code()`。`_6` 已成功生成 3 个代码节点：PDF 第 8 页以及 PPT 第 6、7 页。对照原图，函数、变量、注释和主要语句基本准确。

### 4.11 图片描述偏长并包含知识推测

问题：`_5` 的 43 个图片描述平均约 419 字，最长 902 字；28 项含 Markdown，部分出现“课程核心内容”“通常为”等超出图片本身的推断。描述过长也会增加向量 token 和检索噪声。

修改：提示词要求先标记内容类型，只记录可见文字、对象和明确关系；上下文只用于消歧；禁止 Markdown 和知识补写；公式、表格、代码只概括主题，详细内容由专用流程处理。

`_6` 中平均描述降至约 209 字，Markdown 数量降为 0，明显推测只剩 1 项。密集公式图仍有一项达到 734 字，后续可对正式 Figure 向量描述进一步压缩，同时在 debug 中保留完整描述。

### 4.12 页数少一页

问题：输入实际为 12 页 PDF、13 页 PDF 和 18 页 PPTX，共 43 页，但 `_4` 的 `pages.json` 只有 42 页。PPT 第 18 页为空白页，旧逻辑因为没有文本而不注册 Page。

修改：任何页面都先注册 Page；空白页不生成 TextChunk，但保留页序、前后页关系和文档页数。`_5`、`_6` 均正确输出 43 页。

### 4.13 文本页眉、邮箱和特殊字符噪声

问题：早期文本包含重复课程名、实验室名称、邮箱、页码和 PowerPoint 私有项目符号，影响 TextChunk 和 Page 向量。

修改：按文档统计跨页重复行，默认删除出现在至少 60% 页面中的短页眉页脚；同时清除邮箱、纯页码和私有字符。原始文本保留在 `debug/text/`，正式 Page 和 TextChunk 使用清洗文本。`_6` 中这些噪声已清除。

### 4.14 模型来源和 token 不可追踪

问题：早期日志无法判断每个节点来自 API 还是本地方法，也无法区分图片描述、公式、表格和代码的成本。

修改：节点和 metadata 增加 `backend`、`provider`、`model`、`status`、`api_error`、`indexable`；日志按 `image_description`、`formula`、`table`、`code` 记录调用和 token。

`_6` 的 VLM 使用情况：

| 操作 | 调用次数 | 输入 token | 输出 token | 总 token |
|---|---:|---:|---:|---:|
| 图片描述 | 43 | 38016 | 5514 | 43530 |
| 公式 | 16 | 18383 | 9419 | 27802 |
| 表格 | 3 | 4741 | 1805 | 6546 |
| 代码 | 3 | 2431 | 587 | 3018 |
| 合计 | 65 | 63571 | 17325 | 80896 |

相比 `_5` 的约 185500 VLM token，下降约 56%。

### 4.15 向量生成和向量目标

当前正式节点统一生成 1024 维向量，模型为 `qwen3.7-text-embedding`。向量原料按节点类型组合：文本使用清洗正文和术语，图片使用描述，公式使用 LaTeX，表格使用标题、表头、单位和单元格内容，代码使用语言、描述与源码。

`_6` 成功生成 169 个向量，embedding 输入 token 为 27612；相比 `_5` 的 32623 进一步下降。`vector_index.json` 记录向量与节点的对应关系，`vector_matrix.npy` 按相同顺序保存向量矩阵。

### 4.16 WMF/EMF 转换失败

问题：PPT 第 3 页 WMF 接线图和第 7 页 EMF 资源无法转换。当前机器没有 ImageMagick/Wand，因此被跳过。

处理决定：现已增加跨平台兜底。单个矢量素材优先使用 ImageMagick、Inkscape 或 Wand 转换；仍失败时，Windows 使用 PowerPoint COM、Linux 使用 LibreOffice 将对应幻灯片整页渲染。原矢量文件和失败信息继续保留用于复核。

### 4.17 PaddleOCR 未安装

日志会显示 `PaddleOCR初始化失败`，但当前远程 API 主流程不依赖 PaddleOCR，因此不会导致任务失败。迁移 Linux 后可根据是否需要本地 OCR 兜底再决定安装，当前不列为阻塞问题。

## 5. 当前代码职责

| 文件 | 职责 |
|---|---|
| `src2/multimodal_preprocessor.py` | 总流程、文档处理、图片模态路由 |
| `src2/core/image_processor.py` | 图片增强、通用描述、无关图片判断 |
| `src2/core/formula_extractor.py` | 公式候选提取与来源信息整理 |
| `src2/core/formula_validator.py` | LaTeX 校验、续行合并、去重和审核判断 |
| `src2/core/table_extractor.py` | PDF/PPTX 原生表格和图片表格提取 |
| `src2/core/table_validator.py` | 表格规范化、列宽检查、合并表头修复和审核判断 |
| `src2/core/code_extractor.py` | 文本代码检测和语言判断 |
| `src2/utils/vlm_client.py` | 千问 API 请求、提示词、重试、解析和 token 日志 |
| `src2/utils/output_manager.py` | debug、审核目录和分模态文件输出 |
| `src2/utils/knowledge_exporter.py` | 统一节点、关系和下游交付数据 |
| `src2/utils/vector_index.py` | embedding API 与向量文件生成 |

后续可将各模态 API 提示词进一步拆分到 `src2/recognizers/`，让 `vlm_client.py` 只负责请求和计费统计。但该重构主要改善可维护性，不应优先于识别精度验证。

## 6. 普适性与样本特化风险检查

### 6.1 当前结论

现有实现没有针对当前几个 PDF/PPTX 的文件名、页码、章节号或具体知识内容写死规则。输出隔离、模型溯源、失败审核、公式完整性校验、表格行列校验和分模态识别等主要修改都是通用机制，可以用于不同课程的课件。

但目前还不能认为系统已经对所有 PPT 同等可靠。部分路由词、语言判断和版式假设受到当前机电类、C/C++/Arduino 样本的影响，更准确的定位是“通用框架加少量样本相关启发式规则”，需要跨课程测试集验证。

### 6.2 已具备普适性的部分

1. 图片、公式、表格和代码分别进入专用识别与校验流程。
2. 提示词要求模型忠实转录，不允许利用页面上下文补写不可见内容。
3. API 失败、JSON 截断、低置信度和结构异常结果进入人工审核目录，不直接进入正式 KG 数据。
4. 公式执行括号平衡、LaTeX 完整性、不确定字符、续行和去重检查。
5. 表格执行行列一致性、空单元格比例、合并表头和重复候选检查。
6. 每次运行生成独立递增目录，不覆盖或混合历史结果。
7. 输出记录模型、API、提取方法、置信度、错误和原始资源位置，便于复核与替换模型。

### 6.3 仍然存在的泛化风险

**代码路由偏向 C/C++/Arduino。** 通用的“类型：代码”“程序代码”等标记可覆盖各种语言，但补充关键词包含 `void setup`、`void loop`、`pinMode`、`digitalWrite` 和 `attachInterrupt`。这些并非绑定当前文件的硬编码，但会让 Arduino 代码在通用描述不稳定时更容易被路由，而其他语言缺少同等兜底。

**代码语言覆盖不足。** 当前文本代码检测主要覆盖 Python、Java、C、C++、JavaScript、MATLAB 和 R；对 SQL、TypeScript、HTML/CSS、Shell、Go、Rust、Verilog/VHDL、汇编、PLC 程序和伪代码支持不足。扩展名映射中存在某些语言并不代表检测器能识别该语言。

**无格式代码可能丢行。** 文本代码检测会挑选符合正则的代码行重新组成代码块，注释、宏、数组初始化、多行参数和普通变量声明可能被漏掉。对于图片代码，专用 VLM 返回完整代码；对于 PDF 文本层中的代码，该问题仍需修复。

**模态路由依赖第一次图片描述。** 当前流程是“通用图片描述 -> 判断代码/公式/表格 -> 专用 API”。如果通用描述把代码截图判断成普通文字或流程图，专用代码识别不会启动。这是目前影响跨课程召回率的主要风险。

**公式规则偏数学和工程表达式。** 当前对等式、算术表达式和标准 LaTeX 较有效，但化学方程式、逻辑表达式、集合表达式、证明推导、纯矩阵和统计符号可能漏检。单独符号会被视为非实质公式，这有助于排除变量标签，但某些课程中单独符号本身可能具有教学意义。

**表格存在第一行表头假设。** PPTX 原生表格默认把第一行作为表头。无表头表格、纵向表头、交叉表和复杂多级表头不一定符合该假设，需要保留 `raw_cells` 并通过结构判断或人工审核确认。

**重复图片过滤可能误删有效内容。** 同一图片跨页重复出现至少三次时可能被判断为校徽或水印；如果教师在多页重复展示同一张重要结构图，也可能被误过滤。后续应结合图片面积、页面位置、文本相关性和是否包含知识信息共同判断。

### 6.4 后续泛化改进方向

1. 将模态路由改为“通用视觉分类结果 + 图片版面特征 + 页面对象类型”的联合判断，避免完全依赖自然语言描述关键词。
2. 为代码路由增加语言无关特征，如代码行密度、缩进、括号、运算符、关键字组合和等宽文本布局；Arduino 关键词只作为普通证据之一。
3. 扩展语言检测器，并允许不确定语言以 `unknown` 保存，不能因为语言未知就丢弃代码。
4. 对文本代码先定位连续区域，再整体保存原始行；正则只用于判断区域是否像代码，不用于删掉不匹配的代码行。
5. 为公式增加化学、逻辑、集合、矩阵和统计表达式测试，不按具体课程词汇增加规则。
6. 对表格增加无表头、纵向表头和多级表头判断；无法可靠恢复时保留原始矩阵并进入审核。
7. 重复图片过滤增加保守模式：可疑重复项先进入审核，不因“重复次数”单一条件直接删除。

### 6.5 跨课程验证方案

建立不参与规则调整的独立测试集，至少覆盖：C/C++、Python、Java、SQL、HTML、Verilog 和伪代码；高等数学、物理、化学与统计公式；普通表格、多级表头、无表头、纵向表头和合并单元格表格；照片、结构图、流程图、坐标图及重复教学图片。

分别统计各模态的路由精确率、路由召回率、最终识别准确率和误检率。公式可记录符号级准确率与结构正确率，表格可记录单元格准确率和行列结构正确率，代码可记录标准化编辑距离与完整代码块召回率。只有在独立测试集上保持稳定，才能确认修改具有普适性，而不是仅对当前输入文件有效。

## 7. 下一轮验证重点

下一次完整运行将生成新的递增目录，重点检查：

1. PPT 第 14 页所有公式能否在 2000 token 内完整返回，不再触发部分 JSON 恢复。
2. PPT 第 15 页跨行推导是否合并为完整公式，是否仍保留正确的不确定符号。
3. DM432C 表格第一层表头是否自动恢复为 10 列，并记录 `auto_repairs`。
4. 表格实际数据是否继续保持正确，且不存在错列。
5. PDF 第 8 页代码是否稳定生成，PPT 代码是否不再重复进入公式。
6. 图片描述平均长度和推测性内容是否继续下降。
7. 各 operation 的调用次数和 token 是否合理，公式输出 token 是否因精简 schema 明显下降。
8. `kg_data` 中的页数、节点数、关系数和向量数是否相互一致。

## 8. 当前总体判断

目前抽取流程已经从“能生成结果”进入“按模态校验、失败隔离和成本可追踪”的阶段。文本、普通图片、代码和大多数公式已经具备较好的可用性；表格、密集公式和矢量图兜底仍需要在下一次完整运行中做端到端复核。

## 9. `_7` 后的精度与路径改进

### 9.1 矢量素材兜底

PPTX 中的 EMF、WMF 和 SVG 优先尝试 ImageMagick、Inkscape、Wand 转换。单个素材仍失败时不再删除原文件，而是保留失败记录并渲染对应整页作为视觉理解兜底。Windows 已用真实课件验证 PowerPoint 后台渲染能够生成 2400×1350 PNG；Linux 部署时自动改用 LibreOffice 与 PyMuPDF。

### 9.2 旋转和小字表格

新增独立的 `table_image_preprocessor.py`。视觉表格识别前将小图按比例放大，进行轻量自动对比度和锐化；原方向结构校验失败时才尝试 90° 和 270°，一旦通过立即停止。网络或 API 失败不会触发旋转重试，避免一次超时被放大为三组请求。

### 9.3 向量输入清洗

新增独立的 `embedding_text_cleaner.py`。人工复核使用的原始描述保持不变，只有送入 Embedding API 的 `embedding_text` 会移除校徽、水印、邮箱、网址、角标来源和课程相关性套话。使用 `_7` 的 323 个 Figure 节点回放后，规则检测到的 20 条此类噪声降为 0 条。`vector_index.json` 标记 `embedding_text_policy=cleaned_v1`。

### 9.4 外层工作区相对路径

统一以 `extraction-main` 为路径基准。正式入口从外层目录启动，输入、输出和日志分别使用 `extraction/input/`、`extraction/output/`、`extraction/logs/`。新交付 JSON 中的资源路径统一使用正斜杠并保存为 `extraction/...`；旧结果中的 `input/...`、`output/...` 仍可由补漏程序兼容读取。`course_manifest.json` 与 `schema.json` 增加路径基准和格式声明。

## 10. `_8` 后的代码、表格与公式修复

### 10.1 同页代码防覆盖

此前同一页的多张图片分别触发代码识别时，每次保存都会从 `code_1` 开始编号，导致调试文件和正式 CodeBlock 节点被后一次结果覆盖。现在保存前会查询该页第一个未使用的代码序号，并让源代码文件、metadata 和正式节点使用同一递增编号；断点恢复后也会依据已载入的正式节点继续编号。

### 10.2 多表覆盖与严格审核

视觉表格接口由“每张图片返回一张表”改为“先统计表格区域，再返回全部表格”。响应新增 `visible_table_count`、`extracted_table_count`、`coverage_complete` 和 `table_region_index`。区域数不一致、覆盖未确认、存在不确定单元格或质量分不足的结果均进入人工审核；同一图片含多张表时，即使结构完整也暂不自动进入正式 KG 数据，便于教师逐表核对。表格输出上限由 1500 提升到 3000 token，以容纳密集多表内容。

### 10.3 公式误识别拦截

公式提示词增加字母大小写忠实转录和完整公式约束，并要求模型返回 `is_complete_formula`。确定性校验新增孤立正负号、短尺寸/变量片段、模型自报不完整，以及 `C=C/...` 这类疑似大小写混淆检测。命中规则的内容不会删除，而是保留原图、原响应和原因并进入公式审核目录；图像公式自动通过阈值提高到 0.85。

本轮新增回归测试覆盖重复代码保存、多表保留与审核、表格覆盖不完整、孤立公式片段和大小写混淆，共 13 项测试全部通过。使用 `_8` 结果离线回放，新规则会额外拦截 7 个可疑公式，其中包括 `C = C / \\tau^2`、`\\mp 1` 和 `\\Delta\\theta/2`；其余 4 个为图中的短尺寸表达式，将由教师决定是否保留为独立公式。

## 11. 前端用户与课程任务隔离

前端登录接口已有 session 用户名，课程页面已有 `course_id/course_name`，但当前上传接口尚未把这些身份信息传给 extraction。本轮严格只修改 extraction，没有改动前端或知识图谱仓库。Extraction 现已提供任务身份接收接口，约定后续由前端后端或任务调度层为每次上传生成独立 `job_id`，按用户、课程和任务隔离输入文件，并传入 `job_manifest.json`。

Extraction 正式入口新增 `--job-manifest`、`--input-file`、`--output-dir`、`--user-id`、`--username`、`--course-id`、`--course-name` 和 `--job-id` 参数。身份信息会写入 `course_manifest.json`、文档、页面、内容节点、图谱节点、导出摘要和向量 metadata；断点续跑会从 manifest/checkpoint 恢复同一身份。无参数批量运行仍保持兼容。

建议调用方提供的 manifest 至少包含 `user_id`、`username`、`course_id`、`course_name`、`job_id`、`input_file` 或 `input_dir` 以及可选的 `output_dir`。服务器接入时，后台任务执行器应读取单个任务 manifest，而不是让 extraction 扫描所有用户共享的输入目录。当前前端仓库缺少 Flask 代码所引用的 `models/` 包，因此尚不能完成真实 HTTP 上传端到端测试，实际上传链路由前端同学后续接入。
